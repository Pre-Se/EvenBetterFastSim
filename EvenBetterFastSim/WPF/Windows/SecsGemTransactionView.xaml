<UserControl x:Class="EvenBetterFastSim.WPF.Windows.SecsGemTransactionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:EvenBetterFastSim.WPF.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:SecsGemTransactionViewModel}"
        mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   Text="Modify Transaction"
                   FontSize="16" FontWeight="SemiBold"
                   Margin="0,0,0,20" />

        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="Name" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,0,12,10" />
        <TextBox Grid.Row="1" Grid.Column="1"
                 Margin="0,0,0,10"
                 Text="{Binding TransactionCopy.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Grid.Column="0"
                   Text="Description" VerticalAlignment="Top"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,6,12,0" />
        <TextBox Grid.Row="2" Grid.Column="1"
                 TextWrapping="Wrap" MinHeight="80" AcceptsReturn="True"
                 Text="{Binding TransactionCopy.Description, Mode=TwoWay}" />
    </Grid>
</UserControl>
