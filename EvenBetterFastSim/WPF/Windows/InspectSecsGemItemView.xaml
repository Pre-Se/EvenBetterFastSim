<UserControl x:Class="EvenBetterFastSim.WPF.Windows.InspectSecsGemItemView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:EvenBetterFastSim.WPF.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:InspectSecsGemItemViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="Name" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,0,12,10" />
        <TextBox Grid.Row="1" Grid.Column="1"
                 Margin="0,0,0,10"
                 IsReadOnly="True"
                 Text="{Binding Item.Name}" />

        <TextBlock Grid.Row="2" Grid.Column="0"
                   Text="Type" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,0,12,10" />
        <TextBox Grid.Row="2" Grid.Column="1"
                 Margin="0,0,0,10"
                 IsReadOnly="True"
                 Text="{Binding Item.FormatType}" />

        <TextBlock Grid.Row="3" Grid.Column="0"
                   Text="Value" VerticalAlignment="Top"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,6,12,10"
                   Visibility="{Binding ValueVisibility, Converter={StaticResource BoolToVis}, FallbackValue=Visible}" />
        <TextBox Grid.Row="3" Grid.Column="1"
                 Margin="0,0,0,10"
                 IsReadOnly="True" TextWrapping="Wrap"
                 MaxHeight="200" VerticalScrollBarVisibility="Auto"
                 FontFamily="Cascadia Mono, Consolas"
                 Text="{Binding ValuesDisplay, Mode=OneWay}"
                 Visibility="{Binding ValueVisibility, Converter={StaticResource BoolToVis}, FallbackValue=Visible}" />

        <TextBlock Grid.Row="4" Grid.Column="0"
                   Text="Format" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,0,12,10"
                   Visibility="{Binding IsBinary, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}" />
        <StackPanel Grid.Row="4" Grid.Column="1"
                    Orientation="Horizontal" VerticalAlignment="Center"
                    Margin="0,0,0,10"
                    Visibility="{Binding IsBinary, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
            <RadioButton Content="Hex"
                         IsChecked="{Binding IsHex, Mode=TwoWay}"
                         GroupName="BinaryFormat"
                         Margin="0,0,16,0" />
            <RadioButton Content="Base64"
                         IsChecked="{Binding UseBase64, Mode=TwoWay}"
                         GroupName="BinaryFormat" />
        </StackPanel>

        <TextBlock Grid.Row="5" Grid.Column="0"
                   Text="Description" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,0,12,10" />
        <TextBox Grid.Row="5" Grid.Column="1"
                 Margin="0,0,0,10"
                 IsReadOnly="True"
                 Text="{Binding Item.Description}" />

        <Button Grid.Row="6" Grid.Column="1"
                Content="Download"
                Command="{Binding DownloadItemCommand}"
                Visibility="{Binding IsBinary, Converter={StaticResource BoolToVis}}"
                HorizontalAlignment="Left"
                Padding="16,4" />
    </Grid>
</UserControl>
