<UserControl x:Class="EvenBetterFastSim.WPF.Windows.AddEventReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="CEID" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,0,12,10" />
        <TextBox Grid.Row="1" Grid.Column="1"
                 Margin="0,0,0,10"
                 Text="{Binding Ceid, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Grid.Column="0"
                   Text="Event Name" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,0,12,10" />
        <TextBox Grid.Row="2" Grid.Column="1"
                 Margin="0,0,0,10"
                 Text="{Binding EventName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="3" Grid.Column="0"
                   Text="Active" VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,0,12,10" />
        <CheckBox Grid.Row="3" Grid.Column="1"
                  Margin="0,0,0,10"
                  VerticalAlignment="Center"
                  IsChecked="{Binding IsActive, Mode=TwoWay}" />

        <TextBlock Grid.Row="4" Grid.Column="0"
                   Text="Reports" VerticalAlignment="Top"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   Margin="0,4,12,0" />
        <ListBox Grid.Row="4" Grid.Column="1"
                 MinHeight="120"
                 ItemsSource="{Binding ReportList}"
                 SelectedItem="{Binding SelectedReport, Mode=TwoWay}" />
    </Grid>
</UserControl>
